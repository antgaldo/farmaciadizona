/*
const sidebar = document.getElementById("apri");
const mediasidebar= document.getElementsByClassName("mediasidebar");
const listitem= document.getElementsByClassName("list-group-item");

document.getElementById("btn-apri").addEventListener("click", () => {
    sidebar.classList.toggle("collapsed");
	for (let el of mediasidebar) {
	    el.classList.toggle("dnone");
	}
	for (let el of listitem) {
	    el.classList.toggle("p-1");
	}
});

document.addEventListener("DOMContentLoaded", function () {

    const page = "${page}";

    if(page === "prodotti" && typeof initProdottiPage === "function") {
        initProdottiPage();
    }

    if("dashboard" === "dashboard" && typeof initDashboardPage === "function") {
        initDashboardPage();
    }

});
*/
